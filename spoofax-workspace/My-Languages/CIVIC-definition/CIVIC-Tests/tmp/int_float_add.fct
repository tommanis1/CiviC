initialise-binding initialise-storing initialise-giving finalise-failing finalise-throwing scope
  (collateral
     (bind
        ("main",
         allocate-variable
           (functions
              (tuples
                 ((values)*),
               values)))),
   sequential
     (assign
        (bound
           ("main"),
         function closure
           (scope
              (match
                 (given,
                  tuple
                    ( )),
               handle-return
                 (return
                    (if-else
                       (is-equal
                          (if-else
                             (is-equal
                                (integers,
                                 integers),
                              integers,
                              null),
                           integers),
                        int-add
                          (decimal-natural
                             ("1"),
                           decimal-natural
                             ("1")),
                        float-add
                          (decimal-natural
                             ("1"),
                           decimal-natural
                             ("1")))))))),
      apply
        (assigned
           (bound
              ("main")),
         tuple
           ( ))))